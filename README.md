# Dental AI Chatbot
This project helps Holland Park Dental to capture leads automatically.

## How to Run:
1. Install requirements: `pip install -r requirements.txt`
2. Run the bot: `uvicorn main:app --reload`
3. Access at: `http://127.0.0.1:8000`